export default function PageComponentPlaceholder(_props: any) {
  return (
    <section className="p-6">
      <h1 className="text-2xl font-semibold">BuildingManager / components / ScrollControls</h1>
      <p className="mt-2 text-sm text-slate-600">UI placeholder. Legacy interface was cleared for new project setup.</p>
    </section>
  );
}
